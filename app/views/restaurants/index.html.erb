<div class="text-center container">
  <h1>All Restaurants</h1>
  <%@restaurants.each do |r| %>
    <h5><%= link_to "#{r.name}", restaurant_path(r) %><h5>
  <% end %>
  <%= link_to "Add a new restaurant", new_restaurant_path %>
</div>
